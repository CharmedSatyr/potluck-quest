import { ContactAddressReversed } from "@potluck/utilities/constants";

const ContactAddress = () => (
	<span className="reverse whitespace-pre">{ContactAddressReversed}</span>
);

export default ContactAddress;
