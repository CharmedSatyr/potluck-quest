const NotFoundPage = () => {
	return (
		<section>
			<h1 className="text-primary">Alas, this page eludes us.</h1>
			<p>
				Please check the event code or confirm with the host that it&apos;s
				still happening.
			</p>
		</section>
	);
};

export default NotFoundPage;
