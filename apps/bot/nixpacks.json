{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "nixpacksPlan": {
        "providers": ["node"],
        "phases": {
          "install": {
            "dependsOn": ["setup"],
            "cmds": ["pnpm install --no-frozen-lockfile"]
          }
        }
      }
    }
  }